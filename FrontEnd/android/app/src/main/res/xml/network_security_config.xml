<?xml version="1.0" encoding="utf-8"?>
<network-security-config>

    <!--
        Production : Backend Render — HTTPS uniquement, pas de cleartext
    -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">projet-csn.onrender.com</domain>
    </domain-config>

    <!--
        Développement local : HTTP autorisé pour les IPs locales uniquement
        Émulateur Android   → 10.0.2.2
        Réseau local Wi-Fi  → 192.168.x.x
        Hotspot             → 172.22.x.x
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">192.168.1.180</domain>
        <domain includeSubdomains="false">172.22.211.187</domain>
    </domain-config>

</network-security-config>